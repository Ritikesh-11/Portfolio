# Deployment to Vercel TODO

- [x] Check if Vercel CLI is installed globally
- [x] If not installed, install Vercel CLI using `npm install -g vercel`
- [x] Login to Vercel using `vercel login`
- [ ] Navigate to the `portfolio/` directory
- [ ] Deploy to production using `vercel --prod`
- [ ] Verify the deployment and check for errors
